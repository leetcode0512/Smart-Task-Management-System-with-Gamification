# 🚀 编译和运行指南

## 📋 系统要求

### 必需软件
- **C++编译器**: g++ 7.0+ (支持C++17标准)
- **SQLite3**: 3.0+
- **Make**: GNU Make (可选，用于自动化编译)

### 操作系统
- Linux (推荐)
- macOS
- Windows (WSL或MinGW)

---

## 🔧 安装依赖

### Ubuntu/Debian
```bash
sudo apt-get update
sudo apt-get install g++ make libsqlite3-dev
```

### macOS
```bash
brew install gcc make sqlite3
```

### Windows (WSL)
```bash
# 启用WSL后，使用Ubuntu的安装方式
sudo apt-get install g++ make libsqlite3-dev
```

---

## 🏗️ 编译项目

### 方法1: 使用Makefile (推荐)

#### 编译
```bash
# 进入项目目录
cd Smart-Task-Management-System-with-Gamification

# 编译项目
make

# 或者编译并运行
make run
```

#### 其他Make命令
```bash
# 清理编译文件
make clean

# 调试版本编译
make debug

# 发布版本编译（优化）
make release

# 查看帮助
make help
```

### 方法2: 手动编译

```bash
# 创建输出目录
mkdir -p build bin

# 编译所有源文件
g++ -std=c++17 -Wall -Wextra \
    -I./include -I./common \
    src/main.cpp \
    src/database/databasemanager.cpp \
    src/project/Project.cpp \
    src/project/ProjectManager.cpp \
    src/reminder/ReminderSystem.cpp \
    src/statistics/StatisticsAnalyzer.cpp \
    src/gamification/XPSystem.cpp \
    src/HeatmapVisualizer/HeatmapVisualizer.cpp \
    src/ui/UIManager.cpp \
    -o bin/task_manager \
    -lsqlite3
```

---

## ▶️ 运行程序

### 使用Makefile
```bash
make run
```

### 直接运行
```bash
./bin/task_manager
```

### 首次运行
程序会自动创建数据库文件 `task_manager.db`，并初始化所有必要的表。

---

## 📦 项目结构

```
Smart-Task-Management-System-with-Gamification/
├── common/                         # 公共定义
│   ├── entities.h                 # 实体类定义
│   └── ENTITY_DEFINITION.md       # 实体文档
├── include/                        # 头文件
│   ├── database/                  # 数据库模块
│   │   ├── DatabaseManager.h
│   │   └── DAO/                   # 数据访问对象接口
│   ├── project/                   # 项目管理
│   │   ├── Project.h
│   │   └── ProjectManager.h
│   ├── statistics/                # 统计分析 (成员E)
│   │   └── StatisticsAnalyzer.h
│   ├── gamification/              # 游戏化系统 (成员E)
│   │   └── XPSystem.h
│   ├── HeatmapVisualizer/         # 热力图
│   │   └── HeatmapVisualizer.h
│   └── ui/                        # 用户界面 (成员E)
│       └── UIManager.h
├── src/                            # 源文件
│   ├── main.cpp                   # 主程序 (成员E)
│   ├── database/
│   ├── project/
│   ├── statistics/                # 统计实现 (成员E)
│   ├── gamification/              # XP系统实现 (成员E)
│   ├── HeatmapVisualizer/         # 热力图实现 (成员E)
│   └── ui/                        # UI实现 (成员E)
├── build/                          # 编译临时文件 (自动生成)
├── bin/                            # 可执行文件 (自动生成)
├── Makefile                        # 编译配置
├── README.md                       # 项目说明
├── MEMBER_E_GUIDE.md              # 成员E开发指南
└── BUILD_GUIDE.md                 # 本文档
```

---

## 🎮 功能模块

### ✅ 已实现模块

#### 1. 数据库层 (成员D)
- ✅ DatabaseManager - 数据库连接和管理
- ✅ 表结构定义（tasks, projects, challenges等）
- ⏳ DAO具体实现类 (部分待完成)

#### 2. 项目管理 (成员B)
- ✅ Project类 - 项目实体
- ✅ ProjectManager - 项目管理器
- ✅ 基本CRUD操作

#### 3. 统计分析 (成员E)
- ✅ StatisticsAnalyzer - 统计引擎
- ✅ 任务、时间、生产力统计
- ✅ 报告生成（日报、周报、月报）

#### 4. 游戏化系统 (成员E)
- ✅ XPSystem - 经验值和等级系统
- ✅ 20级等级体系
- ✅ 经验值奖励机制
- ✅ 升级提示

#### 5. 热力图 (成员B + 成员E)
- ✅ HeatmapVisualizer - 热力图生成
- ✅ 90天任务完成可视化
- ✅ 月视图、周视图

#### 6. 用户界面 (成员E)
- ✅ UIManager - UI管理器
- ✅ ANSI彩色控制台界面
- ✅ 主菜单和子菜单系统
- ✅ 项目管理界面
- ✅ 统计分析界面
- ✅ 游戏化功能界面

#### 7. 提醒系统 (成员A)
- ✅ ReminderSystem - 基础框架
- ⏳ 完整功能待实现

### ⏳ 待完成模块

#### 1. 任务管理 (成员C - Kuang Wenqing)
- ❌ TaskManager类
- ❌ 任务CRUD操作
- ❌ 任务状态管理

#### 2. 成就系统 (成员A/D)
- ❌ Achievement管理器
- ❌ 成就解锁逻辑

#### 3. 挑战系统 (成员D)
- ❌ Challenge管理器
- ❌ 挑战进度跟踪

---

## 🧪 测试

### 编译测试
```bash
# 清理并重新编译
make clean
make
```

### 运行测试
```bash
# 运行程序
./bin/task_manager

# 测试功能：
# 1. 创建项目
# 2. 查看统计数据
# 3. 查看经验值和等级
# 4. 查看热力图
```

---

## 🐛 故障排除

### 问题1: "sqlite3.h: No such file or directory"
**解决方案**:
```bash
# Ubuntu/Debian
sudo apt-get install libsqlite3-dev

# macOS
brew install sqlite3
```

### 问题2: "undefined reference to sqlite3_xxx"
**解决方案**: 确保链接了SQLite3库
```bash
# 编译时添加 -lsqlite3
g++ ... -lsqlite3
```

### 问题3: "Permission denied"
**解决方案**: 给予执行权限
```bash
chmod +x bin/task_manager
```

### 问题4: 编译警告
**解决方案**: 大多数警告可以忽略，或根据具体警告调整代码

---

## 📊 当前进度

### 代码统计
- **总代码行数**: ~3500+ 行
- **已实现模块**: 约60%
- **待实现模块**: 约40%

### 成员E贡献
- StatisticsAnalyzer: ~500行
- XPSystem: ~400行
- HeatmapVisualizer: ~350行
- UIManager: ~600行
- main.cpp: ~200行
- **总计**: ~2050行

---

## 📝 注意事项

1. **数据库文件**: 程序首次运行会在当前目录创建 `task_manager.db`
2. **控制台支持**: 需要支持ANSI转义序列的终端（大多数现代终端都支持）
3. **中文显示**: 确保终端编码设置为UTF-8
4. **依赖模块**: 部分功能依赖其他成员的模块，可能显示"功能开发中"

---

## 🤝 团队协作

### 需要协调的事项

#### 与成员C (Kuang Wenqing)
- [ ] TaskManager接口定义和实现
- [ ] 任务完成时XP奖励集成

#### 与成员D (Yu Zhixuan)
- [ ] DAO实现类的完成
- [ ] 数据库查询优化

#### 与成员A (Fei Yifan)
- [ ] 成就系统集成
- [ ] 提醒系统完善

#### 与成员B (Zhou Tianjian)
- [ ] ProjectManager数据库集成
- [ ] 热力图数据源对接

---

## 🎯 下一步开发计划

1. **集成TaskManager** (依赖成员C)
   - 实现任务CRUD界面
   - 完成任务后XP奖励

2. **完善游戏化功能**
   - 成就系统界面
   - 挑战系统界面

3. **数据持久化**
   - 替换ProjectManager的内存存储为数据库
   - 实现DAO类

4. **测试和调试**
   - 完整功能测试
   - Bug修复

---

## 📧 联系方式

如有问题，请联系：
- **成员E (Mao Jingqi)**: 统计、UI、XP系统
- **项目讨论**: GitHub Issues

---

**最后更新**: 2025-11-13
